# Helix V3 — Audit Remediation Plan

Source: full quantitative/execution/architecture audit, 2026-06-12.
Rule of engagement: **no live (demo or real) trading sessions until Tier 0 is complete.**
Nothing gets deleted — superseded code goes through `tools/trash.py` (see bottom).

Status legend: `[ ]` todo · `[~]` in progress · `[x]` done

---

## Tier 0 — Stop the bleeding (live-loss bugs, ~1 day)

- [x] **0.1 Repoint launcher to V2.** `start_helix.py:19` and `pyproject.toml` `[project.scripts]` ran legacy V1. Both now point at `helix_v3.orchestrator_v2`. (Done 2026-06-12.)
- [x] **0.2 Fix false-loss action classification.** (Done 2026-06-12: exit actions classified by explicit prefix `TIME EXIT:`/`STALE EXIT:`/`SESSION EXIT:`; `record_loss` only on parsed negative pips; `record_exit` + replay recording skip TRAIL/T1/STALE TIGHTEN; direction parsed from the action string with active-order/journal fallback. Regression tests cover all 9 action shapes in `tests/test_execution_gatekeeper.py`.) `orchestrator_v2.py:777-810` matches loss markers by substring — `"TRAIL: ... SL->..."` contains `"sl"`, so profitable trail updates record losses and 2 trails = persistent DAY_BAN of a winning direction. Fix: match explicit prefixes only (`"STALE EXIT:"`, `"TIME EXIT:"`, `"SESSION EXIT:"`, SL-hit exits). Also stop calling `guard.record_exit()` for TRAIL/T1/TIGHTEN actions while the position is still open (`orchestrator_v2.py:818-839`). Add regression tests for every action string `gatekeeper` can emit.
- [x] **0.3 Make the order retry loop idempotent.** (Done 2026-06-12: pre-send snapshot of Helix tickets on the symbol; on `None` result the server is reconciled before any retry (full fill → adopt ticket, partial → resend remainder only); `DONE_PARTIAL` resends only the unfilled remainder; `PLACED` never resends (status PENDING, orphan adoption registers a late fill). 4 retry-matrix tests.) `gatekeeper.py:367-433`: on `result is None` or `TRADE_RETCODE_DONE_PARTIAL`, query `mt5.positions_get` filtered by magic+symbol BEFORE any resend; resend only unfilled remainder; treat `TRADE_RETCODE_PLACED` (10008) as success-pending, not failure.
- [x] **0.4 Real kill switch.** (Done 2026-06-12: new `helix_v3/execution/risk_state.py` + `logs/risk_state.db` — balance high-water mark persisted across restarts, daily anchor balance, trips on daily loss >= `MAX_DAILY_LOSS_PCT` (default 4%) or total DD from HWM >= 8%; comparisons use EQUITY so floating losses count; daily trips LATCH for the trading day; gatekeeper notifies via `kill_switch_callback` once/day (wired to the orchestrator notifier). 6 tests in `tests/test_risk_state.py`.) `gatekeeper.py:92-106` uses `(balance-equity)/balance` — blind to realized losses. Add: balance high-water mark persisted to SQLite, max realized daily loss (e.g. 4%), max total drawdown from HWM (8%) → halt new entries + notify. Realized losses must trip it.
- [x] **0.5 Lot-sizing layer 3 must actually reject.** (Done 2026-06-12: `calculate_lot_size` returns `None` when even the broker min lot exceeds the 3% cap (also on zero tick_size/sl), `build_order` aborts on None; lot is FLOORED to volume step, never rounded up; `mt5.order_check()` margin/validity pre-check added before the send loop in `execute_order`. 4 new tests.) `gatekeeper.py:181-191` clamps to `vol_min` and sends anyway. If even `vol_min` exceeds the risk cap → return None, abort the order, log + notify. Also add `mt5.order_check()` margin pre-check, and clamp (not round) lot to step so it can't round up past the cap (`gatekeeper.py:177-179`).
- [x] **0.6 Fix orphan adoption double-T1.** (Done 2026-06-12: adoption detects SL at/past breakeven in the profit direction (1-pip tolerance) → status `T1_HIT`, no T1 reconstruction; pre-T1 positions still get T1 rebuilt at 1:1 RR from SL distance. Tests cover both paths.) `gatekeeper.py:469-488`: if adopted position's SL ≈ entry (breakeven), set status `T1_HIT` and resume trailing — do not reconstruct `take_profit_1 = entry` (which fires a second 50% partial close on every restart).
- [x] **0.7 Quarantine live-firing "tests".** (Done 2026-06-12: `tests/execute_gbpjpy.py` trashed in the first pass; `tests/live_test.py` moved to `tools/manual/live_test.py`; `[tool.pytest.ini_options]` pins `testpaths=["tests"]` + `python_files=["test_*.py"]` so `*_test.py` scripts can never be collected again. `pytest --collect-only` verified clean: 99 tests, none touch live MT5.)
- [x] **0.8 Stop uploading charts to tmpfiles.org.** (Done 2026-06-12: `_upload_to_tmphost` removed entirely; `send_with_chart` is text-only on WhatsApp with a "[Chart available via Telegram]" note; Telegram native photo upload remains the chart channel. Re-enable WhatsApp media only via a private bucket.) `whatsapp.py:185-209` publishes annotated strategy charts (entry/SL/TP) to a public unauthenticated host. Use Telegram's native photo upload (already implemented) or Twilio media via a private bucket; until then, send text-only on WhatsApp.

## Tier 1 — Make the backtest tell the truth (~1-2 weeks)

- [x] **1.1 Kill higher-TF look-ahead.** (Done 2026-06-12: `data_store.get_rates` serves only bars with `open + tf_duration <= as_of`; backtest decision time moved to the CLOSE of the just-finished M15 bar; live `quant_engine.fetch_rates` fetches from position 1, dropping the forming/repainting bar for all consumers. Tests in `tests/test_backtest_data_store.py`.) `backtest/data_store.py:144-149` serves the forming H4/H1/D1 bar with its final OHLC (MT5 indexes by open time). Filter to bars where `open_time + tf_duration <= as_of`. In live mode, drop the forming bar from all pattern/indicator windows (`quant_engine.py:97` `copy_rates_from_pos(..., 0, ...)` includes bar 0).
- [x] **1.2 One canonical timezone module.** (Done 2026-06-12: new `core/market_time.py` — server↔UTC conversion driven by US DST via zoneinfo (GMT+2/+3), vectorized `server_index_to_utc` applied at ingest in BOTH `quant_engine.fetch_rates` and `data_store._fetch_timeframe`; session windows defined once (MMM Book GMT canon), `sessions.py` imports them, `mtf_analyzer` + `quant_engine` use `asian_session_mask` (date-precise, most recent session only), scanner `_get_session_name` delegates, gatekeeper wall-clock fallback now server-stamped. post_asian breach scan restricted to bars AFTER the latest Asian session. Tests in `tests/test_market_time.py`.) `quant_engine.py:104` / `data_store.py:118` stamp MT5 broker server time (ICMarkets GMT+2/+3, DST-switching) as UTC; three contradictory session definitions exist (`quant_engine` vs `sessions.py` vs `mtf_analyzer.py:348-357`). Build `core/market_time.py` with `zoneinfo`, convert server→UTC properly, define Asian/London/NY windows ONCE, import everywhere.
- [x] **1.3 Walk-forward separation for the learning loop.** (Done 2026-06-12: `ValidationLibrary.promote_from_replay`/`rebuild_from_replay` accept `before=` cutoff (outcomes strictly earlier, isoformat-lexicographic); `--validation` backtests now build a separate `validation_library_walkforward.db` from outcomes before `window_start - 7 days` instead of querying the live library; post-run promotion into the live library is opt-in via `--promote`; replay `source_id` is a ms-epoch-seeded monotonic counter — `len(closed_trades)` collided within and ACROSS runs and INSERT OR REPLACE silently overwrote earlier outcomes. Tests in `tests/test_validation_walkforward.py`.) `engine.py:922-940` promotes patterns from a run's own trades into `validation_library.db`, then `engine.py:585-627` queries it on the same data. Add date partitioning: promotion may only use outcomes strictly before the evaluation window; embargo ≥ 1 week between calibration and test. Fix replay `source_id` collisions (`engine.py:702` uses `len(closed_trades)` at entry time).
- [x] **1.4 Honest P&L accounting.** (Done 2026-06-12: P&L = actual computed lot × pip_value_per_lot (lot FLOORED to step); T1 partials book realized cash at closed size immediately, remainder is all TP2/SL can pay; pnl_pips is lot-weighted across legs; risk from equity at entry.) `engine.py:347-359`: use the actual computed lot (step/cap/margin constrained), book T1 partial closes at closed size (currently $0 booked, then full-size TP2 → winners inflated ~43%, `engine.py:304-317`), risk from equity at ENTRY not exit.
- [x] **1.5 Real cost model.** (Done 2026-06-12: half spread + slippage charged on EVERY fill side (full profile spread per round trip), commission per lot per round trip at close; CLI `--slippage` (default 0.5p/side), `--slippage-mult` for 0×/1×/2× stress runs, `--commission`. Historical spread sampling deferred — profile `max_spread_pips` used as the conservative full spread since the live spread gate guarantees entries pay ≤ it.) Full per-pair spread on both sides (sample live spreads from MT5 at signal time and store), commission per lot, swap for overnight, slippage parameter with 0×/1×/2× stress runs. Currently: max_spread/4 on entry only, zero on exits (`engine.py:638-645`).
- [x] **1.6 Gap-aware fills + mark-to-market.** (Done 2026-06-12: SL fills at bar OPEN when gapped through; `_unrealized_pnl` marks the open remainder to the latest close; equity curve and the 8% breaker run on MTM equity with peak tracking; same-bar ordering conservative: SL → T1 → TP2.) SL fills at bar open when gapped through (`engine.py:237` fills at the stop). `_unrealized_pnl` is hardcoded 0.0 (`engine.py:362-364`) — mark open positions to market so drawdown and the 8% breaker see floating excursions. Resolve same-bar T1-vs-TP2 ordering conservatively (T1 first; currently TP2 checked first, `engine.py:229-261`).
- [x] **1.7 Correct statistics.** (Done 2026-06-12: Sharpe now from daily MTM dollar returns (last curve snapshot per UTC day, annualized √252); Deflated Sharpe probability (Bailey & López de Prado) vs the expected max of 50 assumed trials, with skew/kurtosis adjustment; max DD reported in % and $ from the MTM curve including intraday floating excursions. The bogus per-trade-pips Sharpe key is removed entirely. Tests in `tests/test_backtest_report.py`.) `report.py:34-37` computes Sharpe from per-trade pips × √252. Replace with daily dollar-return Sharpe; add Deflated Sharpe Ratio given the number of configurations tried; report max DD from mark-to-market equity.
- [x] **1.8 Fix the stale-tighten no-op.** (Done 2026-06-12: tighten now uses `trade.pip_size` (the old code computed `min_sl/min_sl == 1.0`) and moves `trade.current_sl` — the stop exits actually read — only ever tightening. Regression test proves the tightened stop subsequently fills.) `engine.py:294-301`: `pip_size` computes to 1.0 always, and it writes `trade.stop_loss` while exits read `trade.current_sl`. Align with the replay labeler's correct implementation (`mmm_event_replay.py:592-601`).
- [x] **1.9 RE-RUN the 365-day backtest with 1.1–1.8 in place and accept the number.** (Done 2026-06-12, log in `logs/backtest_365_tier1.log`. **The honest number: -$72.55 (-7.3%) on $1k, 45 trades, 31.1% win rate, PF 0.52, daily-dollar Sharpe -0.10, DSR prob 0.01, max MTM DD 8.2%.** The 8% drawdown breaker tripped in the FIRST month and blocked all entries for the remaining 11 months — the strategy as specified hits its kill line within weeks at realistic costs without look-ahead. Sharpe 4.37 / $1k→$34k is conclusively an artifact. All prior calibration conclusions are void; the edge must be re-derived in Tier 2.)

## Tier 2 — Re-derive the edge honestly (~2-4 weeks)

- [x] **2.1 Restore a real stop-hunt gate.** (Done 2026-06-12: `hunt_detected` now ALWAYS requires a positive breach. Breaches clamped at 0 (a "negative breach" is no breach). M/W soft hunt = 1p..stop_hunt_max breach of the boundary the pattern hunted (W = lows, M = highs) — an M/W with zero breach is a pattern, not a hunt, and a breach deeper than stop_hunt_max stays rejected (the pattern branch can't readmit what the hard gate refused). RRT-only soft hunts take direction from the breach side with the SAME semantics as the hard gate (highs hunted = SELL, lows = BUY) — the old fallback inferred direction from current price, backwards. M/W still overrides breach side per MMM. Tests in `tests/test_mtf_analyzer.py`.) `mtf_analyzer.py:506-511` fabricates `hunt_detected=True` whenever M/W exists (hunt_pips can be negative). Require an actual breach of the Asian range (soft hunts = small but POSITIVE breach + M/W). Fix the contradictory breach-direction semantics (`:439-441` vs `:523-524`).
- [x] **2.2 Re-specify M/W detection.** (Done 2026-06-12: qualifying pattern now needs (1) amplitude floor — neckline-to-extreme ≥ max(5p, 0.25×stop_hunt_min), pair-scaled pips for now, ATR-normalization lands with 2.3; (2) anchoring — the hunted leg must reach the Asian boundary within 0.25×stop_hunt_min; (3) neckline-break confirmation — close back through the middle bump/valley; (4) recency — second leg within the last 8 M15 bars; leg-equality tolerance is stop_hunt_min instead of a fixed 20p. The forming bar is already excluded upstream by Tier 1.1 (live fetch drops bar 0; backtest decides at bar close). Tests in `tests/test_mtf_analyzer.py`.) `mtf_analyzer.py:463-476` is an unanchored 5-bar fractal that fires on noise. Add: amplitude floor (×ATR), anchoring to Asian range/HOD/LOD, neckline-break confirmation, recency bound (pattern within last N bars), and exclude the forming bar.
- [x] **2.3 ATR-normalize every gate.** (Done 2026-06-12: 8 universal `GATE_RATIOS` (fractions of ATR(20, D1)) in `pair_profiles.py` — asian_max 0.55, hunt_min 0.30, hunt_max 1.00, level_move 0.95, trail 0.20/0.15, sl_buffer 0.05, min_sl 0.25 — derived as cross-pair medians from the survey in `tools/manual/atr_ratio_survey.py` (FX medians were tight: the old per-pair constants were ATR proxies all along; gold/indices ratios 0.003-0.07 = vacuous, confirmed). `resolve_profile(symbol, atr)` returns an ATR-resolved profile copy; floors are per-instrument spread facts (buffer>=1 spread, trail>=2, min SL>=4). ATR source `core/volatility.py`: `d1_atr_pips` via quant engine (as-of safe in backtest per Tier 1.1), `d1_atr_pips_mt5` (cached, skips forming bar) for the gatekeeper. Wired: `MTFAnalyzer.analyze` resolves and attaches to `MTFAnalysis.pair_profile` (advisory + backtest runner reuse it), gatekeeper resolves in lot sizing/SL placement/manage loop, `TradeSimulator` freezes the entry-time resolved profile on the trade. Static per-pair pip values KEPT as fallback (ATR unavailable) and for replay/signature bucketing — setup keys must stay comparable with the historical libraries; deleting the constants moves to 2.4 when the signature scheme is rebuilt. The settings-based legacy `quant_engine.detect_stop_hunt` pre-filter is not a pair-profile gate and was left static. Tests in `tests/test_volatility_gates.py`.) Replace fixed pips (Asian range max, hunt min/max, level move, trail, min SL) with ratios of ATR(20, D1). Collapse ~320 per-pair magic numbers to ~10 universal ratios + true per-instrument facts. XAUUSD's 8000/15000-pip vacuous gates disappear by construction (`pair_profiles.py:404-406`).
- [x] **2.4 Rebuild rule validation.** (Done 2026-06-12: new `training/rule_stats.py` — path-ordered `first_touch_outcome` (stop wins same-bar ambiguity), exact binomial tail (no scipy), Benjamini-Hochberg, non-overlapping `empirical_base_rate` sampler. The M/W detector was extracted to module-level `detect_mw_pattern` in `mtf_analyzer.py` so the validator tests the SAME detector production trades. `mw_direction` + `tdi_shark_fin` rebuilt: 1:1 ATR-scaled bracket (Tier 2.3 profiles), non-overlapping outcome windows, binomial vs the empirical unconditional first-touch rate on the same data; `asian_accumulation` tests vs its complement condition, `midweek_reversal` vs other-days rate, `pivot_day_map` vs best constant zone prediction, `stale_exit`/`friday_exit` get sign tests; `stop_hunt_range`/`session_moves`/`adr_bounds` are DESCRIPTIVE (surveys, no claim). BH at q=0.10 across the whole rule×pair grid; verdicts persisted (`p_value`/`base_rate`/`bh_significant`/`verdict` columns). Also fixed: D1 `weekday()` was one day off since Tier 1.2 (bars open 21/22:00 UTC = server midnight) — friday_exit had found ZERO Fridays; now uses the server trading day. **ACCEPTED 180-day verdicts (160 results, log in `logs/rule_validation_tier24.log`): 8 VALIDATED — stale_exit_90min on EURUSD/AUDUSD/EURGBP/EURCHF/GBPCHF/USDCHF, friday_exit on AUDUSD, tdi_shark_fin on GBPCHF only; 99 DEAD including mw_direction, midweek_reversal, pivot_day_map and asian_accumulation on EVERY pair — no directional MMM claim beats its base rate; 3 INSUFFICIENT_N; 50 DESCRIPTIVE. The defensive timing rules are the only validated edge so far.** Tests in `tests/test_rule_stats.py`.) `rule_validator.py:381-389, 633-642` scores hits via MFE>MAE without path ordering (stop-out-then-rally counts as HIT). Use first-touch labeling, non-overlapping samples, binomial tests vs base rates, Benjamini-Hochberg across the rule×pair grid. Rules that fail die.
- [x] **2.5 News blackout.** (Done 2026-06-12: new `core/news_calendar.py` — ForexFactory free weekly JSON (no key), 6h-TTL disk cache at `logs/news_calendar_cache.json`; feed-down degrades to the cached week (scheduled events don't move), no-cache degrades fail-open with loud warnings. `blackout(symbol, at)` matches high-impact events on the pair's currencies (XAUUSD/US30/USTEC = USD) within ±`NEWS_BLACKOUT_MIN` (default 30, env-tunable; `NEWS_BLACKOUT=false` disables). Wired: gatekeeper `build_order` blocks entries in the window; the manage loop closes red positions and moves green ones to breakeven before the event ("NEWS EXIT"/"NEWS BE"). Backtest: `--news-csv time_utc,currency[,title]` injects historical events and blocks entries identically (no historical feed source yet — live protection is the point; the CSV path makes the backtest honest once a historical calendar exists). Verified against the live feed: 13 high-impact events parsed this week, CPI/BOC/ECB at correct UTC. Tests in `tests/test_news_calendar.py`.) No entries ±30min around high-impact events (NFP/CPI/central banks); flatten or widen management around them. A stop-hunt strategy without this is the hunted.
- [x] **2.6 Portfolio currency-exposure cap.** (Done 2026-06-12: new `core/exposure.py` — signed NET risk per currency (BUY EURUSD = +EUR/−USD; gold = XAU/USD; indices own bucket), risk per open position = loss at its CURRENT stop (breakeven stop = 0, no stop = full pair risk), cap = `MAX_CCY_EXPOSURE_MULT` (default 2.0) × `max_risk_per_trade`; a new entry is blocked only when it pushes a bucket it touches further past the cap — offsetting trades always allowed. Wired in BOTH paths: gatekeeper `build_order` (live positions via `mt5.positions_get`) and the backtest runner (simulator open trades, MTM equity); verified end-to-end with a 0.1× cap (138 blocks). Advisory cross-pair convergence bonus capped +12 → +4 (`advisory_confidence.py`): themes are correlated positions, the hard cap is the defense, the bonus stays a mild confirmation. Tests in `tests/test_exposure.py` incl. the audit's 3-GBP-longs example and netting.) Max 2× single-trade risk per base currency across open positions (3 GBP longs = one news candle = ~9% correlated loss today). Remove the advisory bonus for correlated "cross-pair themes" or cap it.
- [x] **2.7 Demote vision LLM from gate to logger.** (Done 2026-06-12: the orchestrator no longer blocks on `consensus.agreed` and no longer fabricates a keyless `agreed=True` — the execution `ConsensusResult` is built purely from the quant pipeline (`analysis.trade_direction`/`trade_confidence`), with the vision verdict carried only as journal metadata (`divergence_notes`). When keys exist, vision runs and its verdict is recorded to `vision_store` (with chart path) for offline calibration; disagreement with the quant direction is logged loudly but never blocks. The file-based verdict bypass is removed entirely (`write_local_verdict`/`_read_local_verdict`/`verdicts/` dir deleted — a file anyone can write must never impersonate a model verdict); `local` mode is now single-API-call-or-declined. Anthropic calls pinned to `temperature: 0` (repeatability, not a determinism guarantee). The 0.88 threshold now only labels the logged advisory `agreed` flag — it gates nothing. Re-enable as a gate only after `logs/vision_backtests.db` shows measured lift over the quant baseline.) Non-deterministic (no temperature/seed, `validator.py:159-177`), anchored prompts, uncalibrated 0.88 self-confidence threshold, fabricated `agreed=True` when keyless (`orchestrator_v2.py:558-570`). Log verdicts + outcomes via `vision_store`; only re-enable as a gate after measured lift over the quant baseline. Remove the file-based verdict bypass for live orders.
- [x] **2.8 Simple regime filter.** (Done 2026-06-12: new `core/regime.py` — two-state PRESENT/ABSENT verdict per symbol from D1: (1) realized-vol percentile = ATR(20, D1) mid-rank vs its trailing 252 days, must sit in [P10, P95] (dead market = nothing to hunt; crisis = hunt geometry meaningless); (2) trendiness = 20-day Kaufman efficiency ratio ≤ 0.50 (a one-way market doesn't cycle back through the Asian range). Reads D1 through the quant engine (as-of safe in backtest per Tier 1.1), cached per D1 bar, fails OPEN on missing data with loud logs. Wired BEFORE any pair-level logic: orchestrator `_process_symbol` step 0.5 and backtest `_check_entry`; toggle `REGIME_FILTER` (default on). Live survey 2026-06-12 (`tools/manual/regime_survey.py`): 10 PRESENT / 6 ABSENT — GBPAUD/EURJPY/EURGBP/GBPCHF/USDJPY dead vol, USTEC crisis vol P100. Tests in `tests/test_regime.py`.) Two-state gate (realized-vol percentile + trendiness) deciding "MMM conditions present/absent" before any pair-level logic.

## Tier 3 — Production hardening (ongoing)

- [x] **3.1 MT5 watchdog.** (Done 2026-06-12: new `execution/mt5_watchdog.py` — tracks last successful broker poll (monotonic), reconnects with exponential backoff (5s→5min, teardown + re-`initialize` with .env creds), dead-man alert through the notifier ONCE per outage after `MT5_DEADMAN_MIN` minutes (default 5) without a good poll, recovery announcement when the connection returns. Wired: orchestrator `_scan_cycle` health-polls `account_info` FIRST and skips the cycle when unhealthy (reconnect runs inside); `manage_open_positions` `positions_get()→None` now logs CRITICAL-grade context ("position management BLIND this cycle" + `mt5.last_error()`) and feeds the watchdog instead of silently returning []. Live-verified: a keyless fresh process auto-reconnected on first poll. Policy logic clock-injectable; tests in `tests/test_mt5_watchdog.py` (dead-man once-per-outage, recovery reset, backoff gating/cap, callback failure isolation).) Reconnect loop with backoff, heartbeat, dead-man alert (no successful broker poll in N minutes → Telegram alert). `positions_get()→None` must log + notify, not silently return [] (`gatekeeper.py:451-453`).
- [x] **3.2 Decouple position management from the slow loop.** (Done 2026-06-12: `_manage_loop` async task runs trailing/T1/stale/news/session exits every `MANAGE_INTERVAL_SEC` (default 10s), started/cancelled by `run()`; the scan cycle no longer touches position management. The manage loop also drives watchdog reconnects when the terminal goes blind (it IS the position-critical path). Action notifications + guard/replay bookkeeping extracted to `_handle_management_actions`. All notification HTTP is now fire-and-forget: new `notifications/dispatch.py` single-worker thread pool (strict FIFO order across notifiers, failures logged in the worker, never raised into the trade path); WhatsApp `_send` and Telegram `_send_text`/`send_with_chart`/`_send_photo_only` split into queueing wrappers + `_sync` impls, internal fallbacks call the sync variants so a fallback can't re-queue out of order. Remaining sync work on the event loop is chart rendering (~1-2s), within the 10s cadence tolerance. Tests in `tests/test_notify_dispatch.py`.) Trailing/T1/stale checks in their own fast async task (every 10-15s); charts, vision, notifications, reports must never delay SL management. Make all notification HTTP fire-and-forget (currently sync httpx, 30s timeouts, inside the trade path — `whatsapp.py:106-111`, `telegram.py:109-131`).
- [x] **3.3 Order-send portability + safety.** (Done 2026-06-12: `_filling_mode` picks from the `symbol_info.filling_mode` bitmask (prefer IOC, then FOK, else RETURN); `_deviation_points` converts a pip budget (max of 1 pip and the pair spread limit) to points per symbol — the hardcoded 10 points was 1 pip on EURUSD but 10 pips on gold; `execute_order` pre-widens SL/TP that sit inside `trade_stops_level` (order_check stays the final authority); `_modify_sl` rewritten — clamps a stop inside the broker minimum to the closest allowed price and SKIPS if clamping would loosen the existing stop, leaves stops inside `freeze_level` alone, rounds to the symbol's digits (fixed 5 was wrong for JPY/gold), and **preserves the position's TP in the request — `TRADE_ACTION_SLTP` with tp omitted CLEARS it, so every trail/breakeven move had been silently wiping TP2** (found during this fix); all pre-checks fail back to the raw modify. Breakeven: T1 retries the BE move once immediately, and a new manage-loop rule 4.5 re-attempts every cycle for any post-T1 position whose stop is not at breakeven, alerting ONCE per ticket ("BE FAILED") and announcing restoration. Tests in `tests/test_order_send_safety.py`.) Select filling mode from `symbol_info.filling_mode` (IOC hardcoded, `gatekeeper.py:364`); validate SL/TP against `trade_stops_level`/`freeze_level` before send/modify; retry failed breakeven `_modify_sl` and ALERT if a post-T1 position is left without breakeven (`gatekeeper.py:706-709`); per-symbol deviation in pips not points (`gatekeeper.py:360`).
- [ ] **3.4 Journal correctness.** `trade_journal.py:438-494`: sum ALL deals per position (T1 partial leg currently excluded from P&L, commission, swap); back-fill broker positions missing from the journal; classify Helix-initiated closes from our own comments, not broker comment substrings.
- [ ] **3.5 Repo hygiene.** Trash V1 orchestrator + dead CLI tools (done via tools/trash.py); single notifier interface (Telegram primary, WhatsApp adapter); charts/ rotation policy (3.5GB and growing); CI running the real test suite; update CLAUDE.md file map (analysis/, watchlist, setup_intelligence, 14 pairs).
- [ ] **3.6 Execution-layer test matrix.** retcode/retry matrix incl. DONE_PARTIAL/PLACED/None, lot-size math per instrument class, orphan adoption (pre/post T1), kill-switch trip conditions, action-string classification (0.2).

---

## Trash workflow (instead of deletion)

- `python tools/trash.py put <path> --reason "..."` — move into `trash/` with manifest entry.
- `python tools/trash.py list` / `restore <id>` — inspect / bring back.
- `python tools/trash_review.py review` — classify every item: still referenced → `restore-recommended`; provable junk (logs/caches/build artifacts) → `delete-candidate` (2h grace, then permanently deleted on a later `review` run); anything not provably junk → `unsure`, maintained forever until a human runs `approve <id>`.

Trashed in the first pass (2026-06-12): V1 `orchestrator.py` (superseded, launcher repointed), `backtest/account_cli_replay.py` (zero importers), `analysis/setup_miner.py` (zero importers), `tests/execute_gbpjpy.py` (live-trade script in pytest path), `hs_err_pid64036.log`, `helix_v3.egg-info/`, `.tmp/`.

Deliberately NOT trashed: `helix_v3/training/` (actively used for vision-review packets), `historical_flashcard_miner.py` / `scanner_replay.py` (active mining work), `tests/live_test.py` (documented integration test — handled in 0.7 instead).
